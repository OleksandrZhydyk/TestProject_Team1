// used for getting user data
export interface UserData {
  id: number;
  username: string;
  email: string;
  is_active: boolean;
  is_superuser: boolean;
  first_name: string;
  last_name: string;
}

