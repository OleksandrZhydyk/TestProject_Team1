import styled from "styled-components";

const StyledNav = styled.nav`
  display: flex;
  align-items: centr;
  justify-content: centr;
  width: 100%;
  border: 1px solid;
`;

const Nav = () => {
  return (
    <StyledNav>
      Nav
    </StyledNav>
  );
};

export default Nav;